/** Repo-content types live in `@tripwire/contracts`; re-exported for `#/lib` imports. */
export type {
	Comment,
	CommentFlag,
	Label,
	RepoContent,
	RepoSummary,
	ThreadDetail,
	ThreadSummary,
	Visibility,
} from "@tripwire/contracts";
