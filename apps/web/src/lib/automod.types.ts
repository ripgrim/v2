/** Automod types live in `@tripwire/contracts`; re-exported for `#/lib` imports. */
export type {
	AutomodMatch,
	AutomodRule,
	AutomodStats,
	MatchVerdict,
	RuleAction,
	RuleCategory,
} from "@tripwire/contracts";
