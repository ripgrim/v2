/**
 * @tripwire/forge — the seam.
 *
 * The `ForgeAdapter` interface + supporting types (`RawForgeEvent`,
 * `ForgeAction`, `DiffFile`, `ContributorProfile`, …). NOTHING ELSE, EVER.
 * No helpers, no base classes, no shared utils. Implementations are siblings
 * (`forge-github`, later `forge-gitlab`) that never import each other.
 *
 * Scaffolded empty in build step 1; the interface lands with the GitHub adapter.
 */
export {};
