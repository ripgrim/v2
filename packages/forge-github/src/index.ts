/**
 * @tripwire/forge-github — the GitHub adapter.
 *
 * Implements `@tripwire/forge`'s `ForgeAdapter`: inbound (verify + normalize),
 * reads (build `RuleContext`), actions (block / label / comment / status,
 * idempotently). Never imports a sibling adapter.
 *
 * Planned surface (spec §4): `adapter.ts`, `webhook/`, `client/`, `actions/`,
 * `fixtures/`. Scaffolded empty in build step 1; built in build step 3.
 */
export {};
