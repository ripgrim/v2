/**
 * @tripwire/ui — design-system primitives only.
 *
 * Button, input, card, dialog, badge, chart shells… lifted from the redesign
 * demo. No app logic, no data fetching, no domain types. Props-driven chrome:
 * a consumer reaching for `className` to change chrome is a smell — expose a
 * prop. Custom app-specific composition lives in `apps/web/components`.
 *
 * Scaffolded empty in build step 1; primitives are lifted as UI work lands.
 */
export {};
